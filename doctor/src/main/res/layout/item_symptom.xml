<androidx.constraintlayout.widget.ConstraintLayout android:id="@+id/symptom_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintRight_toRightOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    android:layout_marginHorizontal="5dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <androidx.cardview.widget.CardView
        android:id="@+id/symptom_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:cardCornerRadius="10dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent">
        <ImageView
            android:id="@+id/symptom_img"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/img_logo"
            android:scaleType="centerCrop" />
    </androidx.cardview.widget.CardView>
    <TextView
        android:id="@+id/symptom_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/PrescriptionSymptomDescText"
        tools:text="1. 잎 끝의 갈변"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/symptom_card"
        app:layout_constraintLeft_toLeftOf="@+id/symptom_card"
        app:layout_constraintRight_toRightOf="@+id/symptom_card"/>
</androidx.constraintlayout.widget.ConstraintLayout>